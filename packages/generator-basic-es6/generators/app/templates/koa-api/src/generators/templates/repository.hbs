import mongoose from '../mongoose'
import {
  {{ camelCase modelName }}Config,
  create{{ properCase modelName }}Model,
} from '{{ modelPackage }}'
import { BaseRepository } from 'mongoose-mask'

export const {{ properCase modelName }} = create{{ properCase modelName }}Model(mongoose)

export default class {{ properCase modelName }}Repository extends BaseRepository {
  constructor() {
    super({{ properCase modelName }}, {{ camelCase modelName }}Config)
  }
}
