import controller from './controller'

import { registerKoaRoutes } from 'mongoose-mask'

export default function registerRoute(router) {
  registerKoaRoutes(router, controller, '{{ dashCase modelName }}')
}
