export create{{ properCase modelName }}Model, {
  schema as {{ camelCase modelName }}Schema,
  schemaName as {{ camelCase modelName }}SchemaName,
  config as {{ camelCase modelName }}Config,
} from './models/{{ modelName }}.model'

// Add export here