import { Schema } from 'mongoose'
import timePlugin from 'mongoose-time-plugin'
import { constants } from 'mongoose-mask'

export const schemaName = '{{ properCase modelName }}'

export const schema = new Schema({
}, { collection: schemaName })

schema.plugin(timePlugin)

// Add Index here

export default (mongoose) => mongoose.model(schemaName, schema)

export const config = {
  key: '_id',
  defaultLimit: 10,
  queryProjection: '',
  detailProjection: '',
  fields: [{
    // filterField: 'headline',
    // dbField: 'headline',
    // compareType: constants.REG_EX_I,
    // dbType: constants.STRING
  }],
}
